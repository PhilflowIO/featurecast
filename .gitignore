node_modules
dist/
test-results/
playwright-report/
.env
.env.*
!.env.example

# Recording artifacts: raw frames and videos do not belong in the repository.
frames/
recordings/
artifacts/
*.webm
*.mp4
*.jpg
*.png
!docs/**/*.png

# Debug logs written by the Playwright MCP server during interactive sessions.
.playwright-mcp/

# Authenticated sessions, cookies, and browser profiles must never be committed.
auth/*
!auth/.gitkeep
playwright/.auth/
